node_modules/
# dist/ is tracked so it can be served via GitHub CDN (jsdelivr)
# Per-package dist is still ignored; only root dist/ is committed
packages/*/dist/
.next/
*.tsbuildinfo
.DS_Store

# Claude Code local settings (contains personal paths + permissions)
.claude/settings.local.json

# Environment files
.env
.env.local
.env.*.local

# OS files
Thumbs.db

# IDE
.vscode/
.idea/
