# Test Report

Snapshot of the workspace's automated test suite. Generated by running
`pnpm test` (`vitest run`) from the repository root.

Last updated: **2026-07-18** (Kiro Session 47 — optional tests added).

This file is a point-in-time snapshot, not a live dashboard — re-run
`pnpm test` for current results; do not assume these numbers still hold
after further code changes.

---

## Summary

| Metric | Value |
|---|---|
| Test files | 8 passed / 8 total |
| Tests | **75 passed** / 75 total |
| Duration | ~0.39s |
| Test runner | Vitest 1.6.0 |
| Command | `pnpm test` (root) → `vitest run` |

| Package | Test file | Tests |
|---|---|---|
| `packages/tokens` | `__tests__/validator.test.ts` | 13 |
| `packages/tokens` | `__tests__/resolver.test.ts` | 7 |
| `packages/tokens` | `__tests__/parser.test.ts` | 8 |
| `packages/tokens` | `__tests__/css-generator.test.ts` | 10 |
| `packages/tokens` | `__tests__/build-pipeline.test.ts` | 4 |
| `packages/css-core` | `__tests__/integration.test.ts` | 12 |
| `packages/generator` | `__tests__/integration.test.ts` | 7 |
| `packages/cli` | `__tests__/parse-args.test.ts` | 14 |

---

## Full Results (`--reporter=verbose`)

### `packages/tokens/__tests__/validator.test.ts` (13)

- ✓ validateTokens > reports valid: true with no errors for a well-formed tree
- ✓ validateTokens > collects errors from multiple independent bad tokens rather than stopping at the first
- ✓ validateTokens > naming-convention > rejects a single-segment identifier
- ✓ validateTokens > naming-convention > rejects an identifier with more than 4 segments
- ✓ validateTokens > naming-convention > accepts identifiers with 2 to 4 segments
- ✓ validateTokens > missing-value / missing-type > reports missing-value when the value field is absent
- ✓ validateTokens > missing-value / missing-type > reports missing-type when the type field is absent
- ✓ validateTokens > reports invalid-type when type is not a recognized TokenType
- ✓ validateTokens > reference validation > reports reference-not-found when a {reference} points at a nonexistent token
- ✓ validateTokens > reference validation > reports reference-type-mismatch when the referenced token has a different type
- ✓ validateTokens > reference validation > does not report reference-not-found or reference-type-mismatch for a valid same-type reference
- ✓ validateTokens > reference validation > reports circular-reference for a two-token cycle
- ✓ validateTokens > reference validation > reports circular-reference for a three-token cycle

### `packages/tokens/__tests__/resolver.test.ts` (7)

- ✓ resolveReferences > passes literal values through unchanged
- ✓ resolveReferences > resolves a single {reference} to the referenced literal value
- ✓ resolveReferences > resolves a multi-level reference chain to the terminal literal
- ✓ resolveReferences > leaves each token unaffected by sibling resolution (no cross-contamination)
- ✓ resolveReferences > resolves a diamond dependency (a→b, a→c, b→d, c→d) to a single consistent value
- ✓ resolveReferences > throws on a circular reference (defense-in-depth; validator is expected to catch this first)
- ✓ resolveReferences > property: any acyclic reference chain resolves fully, leaving no {reference} syntax behind (fast-check property test)

### `packages/tokens/__tests__/parser.test.ts` (8)

- ✓ parseTokenFiles > returns an empty tree for a directory with no .json files
- ✓ parseTokenFiles > throws on invalid JSON with the file path in the message
- ✓ parseTokenFiles > parses deeply nested token structures
- ✓ parseTokenFiles > merges multiple files into a unified tree
- ✓ parseTokenFiles > preserves malformed leaves for validator to catch
- ✓ parseTokenFile > throws when file does not exist
- ✓ parseTokenFile > throws when root value is not an object
- ✓ parser property test > parse produces one leaf per token definition (fast-check)

### `packages/tokens/__tests__/css-generator.test.ts` (10)

- ✓ toCustomPropertyName > prefixes with --ds- and replaces dots with hyphens
- ✓ toCustomPropertyName > converts camelCase segments to kebab-case
- ✓ toCustomPropertyName > handles single-segment identifiers
- ✓ toCustomPropertyName > property: always starts with --ds- (fast-check)
- ✓ toCustomPropertyName > property: never contains dots (fast-check)
- ✓ toCustomPropertyName > property: result is always lowercase (fast-check)
- ✓ generateCSS > produces correct number of custom properties
- ✓ generateCSS > wraps output in :root { ... }
- ✓ generateCSS > sorts properties alphabetically
- ✓ generateCSS > property: output contains exactly N declarations for N tokens (fast-check)

### `packages/tokens/__tests__/build-pipeline.test.ts` (4)

- ✓ build pipeline: invalid input produces no output > validation fails on a token with invalid type
- ✓ build pipeline: invalid input produces no output > validation fails on a token with missing value
- ✓ build pipeline: invalid input produces no output > validation fails on circular reference
- ✓ build pipeline: invalid input produces no output > validation fails on dangling reference

### `packages/css-core/__tests__/integration.test.ts` (12)

- ✓ css-core integration > produces non-empty output
- ✓ css-core integration > starts with @layer declaration
- ✓ css-core integration > contains expected utility selectors
- ✓ css-core integration > contains z-index utilities referencing the zIndex token category
- ✓ css-core integration > contains motion (duration/easing) utilities referencing the motion token category
- ✓ css-core integration > contains border utilities referencing --ds-color-border-main, not a hardcoded value
- ✓ css-core integration > contains responsive variants
- ✓ css-core integration > correctly escapes the digit-leading "2xl" breakpoint prefix
- ✓ css-core integration > contains dark mode selectors
- ✓ css-core integration > uses only var(--ds-*) for visual values in utilities
- ✓ css-core integration > references only custom properties that tokens.css actually defines
- ✓ css-core integration > does not use !important outside print and reduced-motion

### `packages/generator/__tests__/integration.test.ts` (7)

- ✓ generator integration > resolves all 87 tokens
- ✓ generator integration > writes one file per registered transform, plus one per theme
- ✓ generator integration > css-utilities output is byte-identical to the independently-built packages/css-core/dist/core.css
- ✓ generator integration > base tokens.css defines the same custom properties packages/tokens/lib/build.ts does, without inline theme blocks
- ✓ generator integration > each theme file scopes its overrides under [data-theme="<name>"]
- ✓ generator integration > --theme filters which theme files are generated
- ✓ generator integration > --platforms filters which transforms run

### `packages/cli/__tests__/parse-args.test.ts` (14)

- ✓ parseArgs > returns defaults when no arguments provided
- ✓ parseArgs > parses --help flag
- ✓ parseArgs > parses -h shorthand
- ✓ parseArgs > parses --tokens option
- ✓ parseArgs > parses --output option
- ✓ parseArgs > parses --platforms as comma-separated list
- ✓ parseArgs > trims whitespace from platform names
- ✓ parseArgs > parses single --theme option
- ✓ parseArgs > parses multiple --theme options
- ✓ parseArgs > parses --verbose flag
- ✓ parseArgs > parses -v shorthand
- ✓ parseArgs > ignores -- separator (used by pnpm)
- ✓ parseArgs > handles all options combined
- ✓ parseArgs > exits with code 2 on unknown option

---

## Other verification gates (not part of `pnpm test`)

| Command | Result |
|---|---|
| `pnpm build` (tokens → css-core → generator) | ✅ Pass — 87 tokens; `core.css`; 7 generator output files |
| `pnpm run lint` (`scripts/validate-links.mjs`) | ✅ Pass — no broken internal doc links |
| `pnpm --dir apps/roadmap-site build` | ✅ Pass — TypeScript + hardcoded-content check + Next.js |

---

## Known gaps

- `packages/tokens`: JSON and TypeScript generator unit tests still unwritten
  (the generators are exercised indirectly via `packages/generator`'s
  integration test which checks byte-identity of outputs)
- `apps/roadmap-site` has no automated UI/component test suite; its `/demo`
  and `/docs` pages are verified manually during Claude's review passes
